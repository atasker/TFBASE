<div class="holder">
  <p>
    <strong>Email:</strong>
    <%= @user.email %>
  </p>

  <p>
    <strong>Sign in count:</strong> <%= @user.sign_in_count %>
  </p>
  <p>
    <strong>Created at:</strong> <%= @user.created_at.localtime %>
  </p>

  <%#= link_to 'Edit', edit_admin_user_path(@user.id) %>
  <!-- | -->
  <%= link_to 'Back', admin_users_path %>
</div>
