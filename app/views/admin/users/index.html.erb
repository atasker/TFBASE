<div class="holder">
  <h1>Listing Users</h1>

  <%#= link_to 'New user', new_admin_user_path, class: "pure-button" %>

  <!-- <br />
  <br /> -->

  <table class="pure-table pure-table-striped pure-table-horizontal">
    <tr>
      <th>Email</th>
      <th></th>
      <%#<th></th>%>
      <%#<th></th>%>
    </tr>

    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= link_to 'Show', admin_user_path(user.id) %></td>
        <%#<td><= link_to 'Edit', edit_admin_user_path(user.id) ></td>%>
        <%#<td><= link_to 'Destroy', admin_user_path(user.id),
                method: :delete,
                data: { confirm: 'Are you sure?' } ></td>%>
      </tr>
    <% end %>
  </table>
</div>
