Hello, <%= @user.display_name %>

Your order #<%= @order.id %> was successfully created.

Tickets in the order:
<% @order.items.each do |item| %>
* <%= item.ticket_name %> x <%= item.quantity %> = <%= currency_symbol(item) %><%= "%g" % (item.cost) %>
<% end %>

Shipping: <%= currency_symbol(@order) %><%= "%g" % @order.delivery_cost %>
Total: <%= currency_symbol(@order) %><%= "%g" % @order.gross %>

You can always find information about all your orders in your profile on the site.
